name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - '*'
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }} 
  cancel-in-progress: true

jobs:
  unittest:
    runs-on: macos-12
    strategy:
      matrix:
        xcode: ['14.1']
    steps:
      - uses: actions/checkout@v3
      - name: Select Xcode ${{ matrix.xcode }}
        run: sudo xcode-select -s /Applications/Xcode_${{ matrix.xcode }}.app
      - name: setup env
        run: make clean submodules
      - name: Run test in debug
        run: make testdebug
      - name: Run test in production
        run: make testprod